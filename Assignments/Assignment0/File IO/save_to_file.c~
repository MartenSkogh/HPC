#include <stdio.h>
#include <stdlib.h>

#define SIZE 10

int main()
{
  // Allocate memory

  int * asEntries = (int*) malloc(sizeof(int*) * SIZE*SIZE);
  int ** as = (int**) malloc(sizeof(int**) * SIZE);
  
}
